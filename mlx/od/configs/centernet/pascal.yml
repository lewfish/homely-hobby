model:
  type: "centernet"
  centernet:
    backbone_arch: "resnet18"
solver:
  lr: 1e-4
  num_epochs: 10
  sync_interval: 2
  batch_sz: 32
  overfit_num_steps: 400
  test_num_epochs: 1
  one_cycle: True
data:
  dataset: "pascal2007"
  img_sz: 224